# Dentro de las carpeta de backend instalen estas dependencias:

cd mobile

cd backend

# Inicializa el proyecto de Node.js con el siguiente comando
npm init -y

# Instalar varias dependencias esenciales, como Express, Sequelize, MySQL2 (para conectar con MySQL), y algunas herramientas adicionales.

npm install express sequelize mysql2 dotenv cors

#
